const {webpackTemplate} = require("./utils/webpack.template");

/**
 * Create the webpack.config.js file.
 * @param {Object} data An Object with user answer to configurate webpack.config.js file.
 */
module.exports = data => {
}
